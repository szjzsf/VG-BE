[HttpGet]
public IActionResult GetAllItems()
{
    try
    {
        using (var cx = new dbContext())
        {
            var response = cx.Items.ToList();
            return Ok(response);
        }
    }
    catch(Exception ex)
    {
        return BadRequest(ex.Message);
    }
}